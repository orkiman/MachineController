{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(MSVC) Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/machineController.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "internalConsole",
            // "externalConsole": true,
            "preLaunchTask": "CMake Build"
        }
    ]
}
