{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with CMake Preset",
            "type": "shell",
            "command": "cmake --build --preset=msvc-x64-debug",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$msCompile"]
        }
    ]
}
